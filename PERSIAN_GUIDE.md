# 🎉 راهنمای کامل پورتفولیوی ترمینالی

## ✅ تغییرات جدید اعمال شده

### 1. 🖥️ ترمینال تمام‌صفحه
- ترمینال حالا کل صفحه رو پر می‌کنه
- بدون حاشیه یا فاصله
- تجربه immersive و حرفه‌ای
- روی همه سایزهای صفحه کار می‌کنه

### 2. 🖱️ منوی کلیک راست
- هر جای ترمینال رو که کلیک راست کنی، منو باز میشه
- دسترسی سریع به:
  - ⚙️ تنظیمات
  - 🗑️ پاک کردن ترمینال
  - ❓ راهنما
- کلیک چپ هر جا = بستن منو

### 3. 📦 بخش پکیج‌های NPM
- دستور جدید `packages` برای دیدن آمار پکیج‌هات
- نمایش تعداد دانلود:
  - سال گذشته
  - ماه گذشته
  - هفته گذشته
- جمع کل دانلودها
- اطلاعات لحظه‌ای از npm

## 📦 پکیج‌های شما

این پکیج‌ها پیکربندی شدن و آمارشون نمایش داده میشه:

1. **image-auth** - احراز هویت با تصویر
2. **url-validation-query** - اعتبارسنجی URL
3. **react-video-capture** - ضبط ویدیو در React
4. **frontend-stack-cli** - ابزار CLI برای فرانت‌اند
5. **react-providers-tree** - مدیریت Provider در React
6. **react-performanalyzer** - تحلیل عملکرد React
7. **sse-shared-worker-react-hook** - Hook برای SSE
8. **vite-plugin-react-splash** - پلاگین Splash برای Vite

## 🎮 نحوه استفاده

### دیدن آمار پکیج‌ها

```bash
# تو ترمینال بنویس:
packages

# یا برای رفرش کردن:
packages --refresh
```

خروجی چیزی شبیه این میشه:

```
📦 NPM Packages Statistics:
═══════════════════════════════════════════════════════

  1. image-auth
     📥 Last Year:  12,345
     📅 Last Month: 1,234
     📆 Last Week:  123

  ... (بقیه پکیج‌ها)

═══════════════════════════════════════════════════════
📊 Total Downloads (Last Year):  123,456
📊 Total Downloads (Last Month): 12,345
📊 Total Downloads (Last Week):  1,234
```

### باز کردن تنظیمات

**روش ۱:** کلیک راست → انتخاب "Settings"

**روش ۲:** کلیک روی آیکون ⚙️ بالا سمت راست

**روش ۳:** تایپ کردن `settings`

### منوی کلیک راست

- کلیک راست هر جا = باز شدن منو
- کلیک چپ هر جا = بسته شدن منو
- گزینه‌ها:
  - تنظیمات
  - پاک کردن ترمینال
  - راهنما

## 🔧 فایل‌های مهم

### 1. Terminal.tsx
مسیر: `/app/components/Terminal.tsx`

کامپوننت اصلی ترمینال که شامل:
- لایه تمام‌صفحه
- منوی کلیک راست
- نمایش آمار پکیج‌ها
- تنظیمات به صورت overlay

### 2. npmStats.ts (جدید)
مسیر: `/app/lib/npmStats.ts`

ابزار برای دریافت آمار npm:
- دریافت تعداد دانلودها
- محاسبه جمع کل
- فرمت کردن اعداد
- لیست پکیج‌ها

## ⚠️ نکته مهم درباره آمار ۵ ساله

API رسمی npm فقط آمار **۱۸ ماه گذشته** رو داره!

برای دیدن آمار ۵ سال گذشته:

### راه حل ۱: استفاده از سرویس‌های شخص ثالث
- [npm-stat.com](https://npm-stat.com)
- [npmtrends.com](https://npmtrends.com)

### راه حل ۲: ساخت سیستم ردیابی خودت
- یه cron job بنویس که روزانه آمار رو بگیره
- تو دیتابیس ذخیره کن
- نمودار تاریخی بساز

### راه حل ۳: جمع‌آوری دستی
- از سایت‌های npm-stat.com آمار تاریخی بگیر
- دستی جمع‌آوری کن

### پیاده‌سازی فعلی:
- ✅ آمار سال گذشته (۳۶۵ روز)
- ✅ آمار ماه گذشته (۳۰ روز)
- ✅ آمار هفته گذشته (۷ روز)
- ✅ جمع کل همه پکیج‌ها

## 🎨 شخصی‌سازی

### اضافه/حذف کردن پکیج‌ها

فایل `/app/lib/npmStats.ts` رو باز کن:

```typescript
export const MY_PACKAGES = [
  "اسم-پکیج-اول",
  "اسم-پکیج-دوم",
  // پکیج‌های بیشتر اینجا
];
```

### تغییر منوی کلیک راست

فایل `/app/components/Terminal.tsx` رو باز کن و قسمت context menu رو پیدا کن:

```typescript
{contextMenu && (
  <div>
    {/* آیتم‌های منوی خودت رو اینجا اضافه کن */}
  </div>
)}
```

### تغییر حالت تمام‌صفحه

اگه می‌خوای دور ترمینال فاصله باشه:

```typescript
// از این:
<div className="h-screen w-screen overflow-hidden bg-gray-900">

// به این تغییر بده:
<div className="min-h-screen p-4 bg-gray-900">
```

## 🚀 نحوه اجرا

### ۱. شروع سرور

سرور در حال حاضر روی پورت ۵۱۷۳ در حال اجراست:

```
http://localhost:5173/
```

اگه نیاز به ری‌استارت داشتی:

```bash
cd /Users/mohammadgarmabi/Documents/My\ Projects/my-repo/portfolio
npm run dev
```

### ۲. مشاهده در مرورگر

مرورگر رو باز کن و برو به:
```
http://localhost:5173/
```

### ۳. تست کردن

دستورات زیر رو امتحان کن:

```bash
help        # لیست دستورات
packages    # آمار پکیج‌ها
settings    # تنظیمات
clear       # پاک کردن صفحه
```

## 📝 دستورات موجود

```bash
help       - نمایش همه دستورات
about      - درباره من
skills     - مهارت‌ها
projects   - پروژه‌ها
packages   - آمار پکیج‌های npm (جدید!)
experience - تجربیات کاری
education  - تحصیلات
contact    - اطلاعات تماس
settings   - تنظیمات
clear      - پاک کردن ترمینال
```

## ⌨️ کلیدهای میانبر

| کلید | عملکرد |
|------|--------|
| `Tab` | جابجایی بین پیشنهادات |
| `↑` | دستور قبلی |
| `↓` | دستور بعدی |
| `Cmd/Ctrl + L` | پاک کردن ترمینال |
| `Enter` | اجرای دستور |
| `کلیک راست` | باز کردن منو |

## 🐛 رفع مشکل

### آمار پکیج‌ها نمایش داده نمیشه؟

1. اینترنت رو چک کن
2. اسم پکیج‌ها رو تو `npmStats.ts` بررسی کن
3. کنسول مرورگر رو چک کن
4. دستور `packages --refresh` رو امتحان کن

### منوی کلیک راست باز نمیشه؟

1. مطمئن شو تو ناحیه ترمینال کلیک راست می‌کنی
2. ببین مرورگر منوی کلیک راست رو بلاک نکرده
3. به جاش روی آیکون ⚙️ کلیک کن

### تنظیمات باز نمیشه؟

1. روی آیکون ⚙️ بالا سمت راست کلیک کن
2. یا کلیک راست → Settings
3. یا دستور `settings` رو بنویس

## 🎯 کارهای بعدی (اختیاری)

### پیشنهادات برای بهبود:

1. **ردیابی آمار تاریخی:**
   - یه بک‌اند بساز که روزانه آمار رو ذخیره کنه
   - نمودار و چارت بساز
   - امکان export به CSV

2. **اطلاعات بیشتر پکیج:**
   - نمایش ورژن پکیج
   - نمایش توضیحات
   - لینک به صفحه npm
   - نمایش dependencies

3. **آمار پیشرفته:**
   - روند دانلودها
   - نمودار مقایسه
   - محاسبه نرخ رشد
   - محبوب‌ترین ورژن‌ها

4. **کش کردن:**
   - ذخیره آمار در localStorage
   - رفرش خودکار هر ساعت
   - نمایش آخرین به‌روزرسانی

## 📊 عملکرد

- آمار پکیج‌ها فقط وقتی fetch میشن که دستور `packages` رو بزنی
- از Promise.all برای فراخوانی موازی استفاده میشه
- نتایج تو session کش میشن
- بدون fetch خودکار در پس‌زمینه (صرفه‌جویی در API calls)

## 🎉 خلاصه

پورتفولیوی شما الان این قابلیت‌ها رو داره:

- ✅ ترمینال تمام‌صفحه
- ✅ منوی کلیک راست
- ✅ آمار پکیج‌های npm
- ✅ تنظیمات به صورت overlay
- ✅ دریافت لحظه‌ای از npm API
- ✅ ۸ پکیج پیکربندی شده
- ✅ آمار دانلود (سال/ماه/هفته)
- ✅ محاسبه جمع کل

**همه چیز آماده و کار می‌کنه!** 🚀

## 📚 مستندات

سه فایل راهنما آماده شده:

1. **README.md** - توضیحات کلی (انگلیسی)
2. **CUSTOMIZATION.md** - راهنمای شخصی‌سازی (انگلیسی)
3. **UPDATES.md** - تغییرات جدید (انگلیسی)
4. **PERSIAN_GUIDE.md** - این فایل (فارسی)

## 🌟 نکات مهم

### درباره محدودیت ۵ سال:

API رسمی npm فقط ۱۸ ماه آمار داره. برای دیدن آمار ۵ ساله واقعی باید:

1. از سرویس شخص ثالث استفاده کنی
2. سیستم ردیابی خودت رو بسازی
3. یا آمار تاریخی رو دستی جمع‌آوری کنی

پیاده‌سازی فعلی جدیدترین و دقیق‌ترین اطلاعات موجود از API رسمی npm رو نشون میده.

### آماده برای استفاده:

همه چیز کار می‌کنه و آماده نمایشه! فقط کافیه:

1. مرورگر رو باز کنی
2. به `http://localhost:5173/` بری
3. دستور `packages` رو بزنی
4. آمار پکیج‌هات رو ببینی!

---

**موفق باشی!** 🎊

اگه سوالی داشتی یا کمک خواستی، بگو! 💻✨

